---
- name: Add security rule to Panorama
  paloaltonetworks.panos.panos_security_rule:
    provider: "{{ provider }}"
    rule_name: "{{ rule_name }}"
    description: "{{ description }}"
    source_zone: "{{ source_zone }}"
    destination_zone: "{{ destination_zone }}"
    source_ip: "{{ source_ip }}"
    destination_ip: "{{ destination_ip }}"
    application: "any"
    service: "{{ service }}"
    action: "{{ action }}"
    device_group: "{{ device_group }}"
    commit: false  # Commit separately later

- name: Commit the changes to Panorama
  paloaltonetworks.panos.panos_commit_panorama:
    provider: "{{ provider }}"
    sync: true
  register: panorama_commit_result

- name: Commit all changes from Panorama to firewalls
  paloaltonetworks.panos.panos_commit_firewall:
    provider: "{{ provider }}"
    exclude_device_and_network: true
  when:
    - panorama_commit_result is succeeded
